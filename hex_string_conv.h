#ifndef HEX_CONV_HEADER
#define HEX_CONV_HEADER

#include <string>
using namespace std; 
void hex_to_binary(string text, unsigned char *output, unsigned int N);
void binary_to_hex(unsigned char *text, unsigned int N, string &output);

#endif
